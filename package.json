{
  "name": "brain-starter",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "setup": "pnpm install && echo 'Done! Run: git init && git add -A && git commit -m \"chore: initial brain setup\"'"
  },
  "packageManager": "pnpm@10.32.1",
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7"
  }
}
